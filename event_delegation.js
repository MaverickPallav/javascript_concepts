// Instead of attaching event handlers to each child, add event handler to parent of the childs

// Event attached to child will bubble out to the parent event listener and callback function is called

